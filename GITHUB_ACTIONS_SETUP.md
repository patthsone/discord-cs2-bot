# GitHub Actions Setup Guide

## 🚀 Настройка секретов для GitHub Actions

Для работы GitHub Actions workflows необходимо добавить следующие секреты в настройках репозитория:

### 📍 Где добавить секреты:
1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **Settings** (Настройки)
3. В левом меню выберите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### ⚠️ **ВАЖНО: Деплой опционален!**

**Если вы не настроили секреты для деплоя:**
- ✅ CI workflows будут работать нормально
- ✅ Тесты и проверки будут проходить
- ⚠️ CD workflow пропустит деплой с предупреждением
- 📝 Это нормально для разработки!

### 🔐 Обязательные секреты для деплоя:

#### **DEPLOY_HOST**
- **Описание**: IP адрес или домен вашего сервера
- **Пример**: `192.168.1.100` или `myserver.com`
- **Как получить**: IP адрес вашего VPS/сервера

#### **DEPLOY_USER**
- **Описание**: Имя пользователя для SSH подключения
- **Пример**: `ubuntu`, `root`, `deploy`
- **Как получить**: Обычно `ubuntu` для AWS, `root` для других VPS

#### **DEPLOY_KEY**
- **Описание**: Приватный SSH ключ для подключения к серверу
- **Пример**: Содержимое файла `~/.ssh/id_rsa`
- **Как получить**: 
  ```bash
  # Создать SSH ключ (если нет)
  ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
  
  # Скопировать приватный ключ
  cat ~/.ssh/id_rsa
  ```

#### **DEPLOY_PATH**
- **Описание**: Путь к директории приложения на сервере
- **Пример**: `/home/ubuntu/discord-cs2-bot`
- **Как получить**: Путь где вы хотите разместить бота на сервере

### 🐳 Секреты для Docker (опционально):

#### **DOCKER_USERNAME**
- **Описание**: Имя пользователя Docker Hub
- **Пример**: `yourusername`
- **Получить**: https://hub.docker.com/

#### **DOCKER_PASSWORD**
- **Описание**: Пароль или токен доступа Docker Hub
- **Пример**: `yourpassword` или `dckr_pat_...`
- **Получить**: Docker Hub → Account Settings → Security

### 🔒 Секреты для безопасности (опционально):

#### **SNYK_TOKEN**
- **Описание**: Токен для Snyk security scanning
- **Получить**: https://snyk.io/account
- **Пример**: `your-snyk-token`

## 📋 Переменные окружения для бота:

Добавьте также переменные окружения для работы бота:

### **DISCORD_TOKEN**
- **Описание**: Токен Discord бота
- **Получить**: https://discord.com/developers/applications

### **DATABASE_URL**
- **Описание**: Строка подключения к MySQL
- **Пример**: `mysql://user:password@localhost:3306/discord_cs2_bot`

### **REDIS_HOST**
- **Описание**: Хост Redis сервера
- **Пример**: `localhost` или `redis.example.com`

## ✅ Проверка настройки:

После добавления секретов:

1. **CI Only Workflow** - работает всегда (тесты, проверки)
2. **CI Workflow** - работает всегда (тесты, линтинг, сборка)
3. **CD Workflow** - работает только при наличии секретов деплоя
4. **Security Workflow** - работает еженедельно

## 🔍 Мониторинг:

- Перейдите в раздел **Actions** вашего репозитория
- Там вы увидите все запущенные workflows
- Зеленые галочки = успех, красные крестики = ошибка

## 🛠️ Локальная разработка:

Для локальной разработки скопируйте `env.github-actions.example` в `.env` и заполните значениями для вашего окружения.

## 🚨 Решение проблем:

### Ошибка "Could not resolve hostname":
- Проверьте правильность DEPLOY_HOST
- Убедитесь что сервер доступен
- Проверьте DNS настройки

### Ошибка "Connection closed":
- Проверьте DEPLOY_USER
- Убедитесь что SSH ключ правильный
- Проверьте права доступа на сервере

### Ошибка "Permission denied":
- Проверьте DEPLOY_PATH
- Убедитесь что пользователь имеет права на директорию
- Проверьте права на файлы

---

**Примечание**: Никогда не коммитьте файлы с секретами в репозиторий! Используйте только GitHub Secrets.
